{
  "name": "goaliq",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "build": "node --check app.js && node --check scripts/update-fixtures.mjs && node --check scripts/worldcup-bracket.mjs && node scripts/validate-form-data.mjs",
    "test": "node --test tests/*.test.mjs && node scripts/validate-form-data.mjs"
  }
}
